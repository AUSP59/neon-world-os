name: determinism-guard
on: [push, pull_request]
permissions: { contents: read }
jobs:
  grep-nondeterminism:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - run: bash tools/grep_nondeterminism.sh