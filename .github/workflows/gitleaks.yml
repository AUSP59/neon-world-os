name: Gitleaks
on: [push, pull_request]
permissions: { contents: read }
jobs:
  scan:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: gitleaks/gitleaks-action@v2
        with: { args: detect --no-git -v }